-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.__drizzle_migrations (
  id integer NOT NULL DEFAULT nextval('__drizzle_migrations_id_seq'::regclass),
  hash text NOT NULL,
  created_at bigint,
  CONSTRAINT __drizzle_migrations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.accounts (
  id text NOT NULL,
  user_id text NOT NULL,
  type text NOT NULL,
  provider text NOT NULL,
  provider_account_id text NOT NULL,
  refresh_token text,
  access_token text,
  expires_at integer,
  token_type text,
  scope text,
  id_token text,
  session_state text,
  CONSTRAINT accounts_pkey PRIMARY KEY (id),
  CONSTRAINT accounts_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.attribute_values (
  id text NOT NULL,
  attribute_id text NOT NULL,
  value text NOT NULL,
  position integer DEFAULT 0,
  is_featured boolean DEFAULT false,
  locale text DEFAULT 'en'::text,
  meta jsonb,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT attribute_values_pkey PRIMARY KEY (id)
);
CREATE TABLE public.attributes (
  id text NOT NULL,
  name text NOT NULL,
  slug text NOT NULL UNIQUE,
  position integer DEFAULT 0,
  is_featured boolean DEFAULT false,
  locale text DEFAULT 'en'::text,
  meta jsonb,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT attributes_pkey PRIMARY KEY (id)
);
CREATE TABLE public.bundle_deals (
  id text NOT NULL DEFAULT (gen_random_uuid())::text,
  name text NOT NULL,
  description text,
  discount_type text NOT NULL DEFAULT 'percentage'::text CHECK (discount_type = ANY (ARRAY['percentage'::text, 'fixed'::text])),
  discount_value numeric NOT NULL,
  min_items integer DEFAULT 2,
  max_items integer,
  is_active boolean DEFAULT true,
  starts_at timestamp without time zone,
  ends_at timestamp without time zone,
  image text,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT bundle_deals_pkey PRIMARY KEY (id)
);
CREATE TABLE public.bundle_items (
  id text NOT NULL DEFAULT (gen_random_uuid())::text,
  bundle_id text NOT NULL,
  product_id text NOT NULL,
  required_quantity integer DEFAULT 1,
  optional boolean DEFAULT false,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT bundle_items_pkey PRIMARY KEY (id),
  CONSTRAINT bundle_items_bundle_id_fkey FOREIGN KEY (bundle_id) REFERENCES public.bundle_deals(id)
);
CREATE TABLE public.cart_items (
  id text NOT NULL DEFAULT (gen_random_uuid())::text,
  user_id text,
  session_id text,
  product_id text NOT NULL,
  variant_id text,
  quantity integer NOT NULL DEFAULT 1,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT cart_items_pkey PRIMARY KEY (id),
  CONSTRAINT cart_items_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.carts (
  id text NOT NULL,
  user_id text,
  session_id text,
  position integer DEFAULT 0,
  locale text DEFAULT 'en'::text,
  meta jsonb,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT carts_pkey PRIMARY KEY (id)
);
CREATE TABLE public.categories (
  id text NOT NULL DEFAULT (gen_random_uuid())::text,
  name text NOT NULL,
  slug text NOT NULL UNIQUE,
  description text,
  image text,
  parent_id text,
  is_active boolean DEFAULT true,
  sort_order integer DEFAULT 0,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  materialized_path text,
  position integer DEFAULT 0,
  is_featured boolean DEFAULT false,
  locale text DEFAULT 'ru'::text,
  CONSTRAINT categories_pkey PRIMARY KEY (id),
  CONSTRAINT categories_parent_id_fkey FOREIGN KEY (parent_id) REFERENCES public.categories(id)
);
CREATE TABLE public.collection_items (
  id text NOT NULL,
  collection_id text NOT NULL,
  product_id text NOT NULL,
  sort_order integer DEFAULT 0,
  added_by text,
  created_at timestamp without time zone NOT NULL DEFAULT now(),
  CONSTRAINT collection_items_pkey PRIMARY KEY (id),
  CONSTRAINT collection_items_added_by_fkey FOREIGN KEY (added_by) REFERENCES public.users(id),
  CONSTRAINT collection_items_collection_id_fkey FOREIGN KEY (collection_id) REFERENCES public.curated_collections(id)
);
CREATE TABLE public.coupons (
  id text NOT NULL,
  code text NOT NULL UNIQUE,
  discount integer NOT NULL,
  type text NOT NULL DEFAULT 'percent'::text,
  min_order numeric,
  max_uses integer,
  used_count integer DEFAULT 0,
  active boolean DEFAULT true,
  expires_at timestamp without time zone,
  created_by text,
  created_at timestamp without time zone NOT NULL DEFAULT now(),
  updated_at timestamp without time zone NOT NULL DEFAULT now(),
  CONSTRAINT coupons_pkey PRIMARY KEY (id),
  CONSTRAINT coupons_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id)
);
CREATE TABLE public.curated_collections (
  id text NOT NULL,
  name text NOT NULL,
  slug text NOT NULL UNIQUE,
  description text,
  cover_image text,
  is_active boolean NOT NULL DEFAULT true,
  sort_order integer DEFAULT 0,
  created_by text,
  created_at timestamp without time zone NOT NULL DEFAULT now(),
  updated_at timestamp without time zone NOT NULL DEFAULT now(),
  CONSTRAINT curated_collections_pkey PRIMARY KEY (id),
  CONSTRAINT curated_collections_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id)
);
CREATE TABLE public.email_templates (
  id text NOT NULL,
  name text NOT NULL UNIQUE,
  subject text NOT NULL,
  html_content text NOT NULL,
  text_content text,
  category text NOT NULL DEFAULT 'marketing'::text,
  active boolean DEFAULT true,
  variables ARRAY,
  created_by text,
  created_at timestamp without time zone NOT NULL DEFAULT now(),
  updated_at timestamp without time zone NOT NULL DEFAULT now(),
  CONSTRAINT email_templates_pkey PRIMARY KEY (id),
  CONSTRAINT email_templates_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id)
);
CREATE TABLE public.gift_cards (
  id text NOT NULL DEFAULT (gen_random_uuid())::text,
  code text NOT NULL UNIQUE,
  amount numeric NOT NULL,
  balance numeric NOT NULL,
  status text NOT NULL DEFAULT 'active'::text CHECK (status = ANY (ARRAY['active'::text, 'used'::text, 'expired'::text, 'disabled'::text])),
  purchaser_id text,
  recipient_email text,
  recipient_name text,
  message text,
  expires_at timestamp without time zone,
  redeemed_at timestamp without time zone,
  redeemed_by text,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT gift_cards_pkey PRIMARY KEY (id),
  CONSTRAINT gift_cards_purchaser_id_fkey FOREIGN KEY (purchaser_id) REFERENCES public.users(id),
  CONSTRAINT gift_cards_redeemed_by_fkey FOREIGN KEY (redeemed_by) REFERENCES public.users(id)
);
CREATE TABLE public.link_events (
  id text NOT NULL,
  entity_type text NOT NULL,
  entity_id text NOT NULL,
  action text NOT NULL,
  old_values jsonb,
  new_values jsonb,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT link_events_pkey PRIMARY KEY (id)
);
CREATE TABLE public.maintenance_subscriptions (
  id text NOT NULL DEFAULT gen_random_uuid(),
  email text NOT NULL UNIQUE,
  subscribed_at timestamp with time zone DEFAULT now(),
  notified boolean DEFAULT false,
  notified_at timestamp with time zone,
  CONSTRAINT maintenance_subscriptions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.media (
  id text NOT NULL,
  url text NOT NULL,
  alt text,
  entity_type text NOT NULL,
  entity_id text NOT NULL,
  role text DEFAULT 'gallery'::text,
  position integer DEFAULT 0,
  locale text DEFAULT 'en'::text,
  meta jsonb,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT media_pkey PRIMARY KEY (id)
);
CREATE TABLE public.order_items (
  id text NOT NULL DEFAULT (gen_random_uuid())::text,
  order_id text NOT NULL,
  product_id text NOT NULL,
  product_name text NOT NULL,
  variant_id text,
  sku text,
  quantity integer NOT NULL,
  price numeric NOT NULL,
  total numeric NOT NULL,
  size text,
  color text,
  image text,
  created_at timestamp without time zone DEFAULT now(),
  name text,
  CONSTRAINT order_items_pkey PRIMARY KEY (id),
  CONSTRAINT order_items_order_id_fkey FOREIGN KEY (order_id) REFERENCES public.orders(id)
);
CREATE TABLE public.order_promotions (
  id text NOT NULL,
  order_id text NOT NULL,
  promotion_id text NOT NULL,
  valid_from timestamp without time zone,
  valid_to timestamp without time zone,
  position integer DEFAULT 0,
  locale text DEFAULT 'en'::text,
  created_by text,
  reason text,
  meta jsonb,
  deleted_at timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT order_promotions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.orders (
  id text NOT NULL DEFAULT (gen_random_uuid())::text,
  order_number text UNIQUE,
  user_id text,
  guest_email text,
  status text NOT NULL DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'processing'::text, 'shipped'::text, 'delivered'::text, 'cancelled'::text, 'returned'::text])),
  payment_status text NOT NULL DEFAULT 'pending'::text CHECK (payment_status = ANY (ARRAY['pending'::text, 'paid'::text, 'failed'::text, 'refunded'::text])),
  subtotal numeric NOT NULL,
  tax numeric DEFAULT 0,
  shipping_cost numeric DEFAULT 0,
  discount numeric DEFAULT 0,
  total numeric NOT NULL,
  currency text DEFAULT 'RUB'::text,
  payment_method text,
  payment_intent_id text,
  shipping_address jsonb,
  billing_address jsonb,
  customer_note text,
  admin_note text,
  tracking_number text,
  tracking_url text,
  shipped_at timestamp without time zone,
  delivered_at timestamp without time zone,
  cancelled_at timestamp without time zone,
  cancelled_by text,
  cancel_reason text,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  delivery_price numeric DEFAULT 0,
  delivery_method text,
  recipient text,
  comment text,
  transaction_id text,
  crypto_currency text,
  crypto_address text,
  crypto_tx_id text,
  crypto_amount numeric,
  CONSTRAINT orders_pkey PRIMARY KEY (id),
  CONSTRAINT orders_cancelled_by_fkey FOREIGN KEY (cancelled_by) REFERENCES public.users(id),
  CONSTRAINT orders_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.product_attribute_values (
  id text NOT NULL,
  product_id text NOT NULL,
  attribute_value_id text NOT NULL,
  valid_from timestamp without time zone,
  valid_to timestamp without time zone,
  position integer DEFAULT 0,
  is_featured boolean DEFAULT false,
  locale text DEFAULT 'en'::text,
  created_by text,
  reason text,
  meta jsonb,
  deleted_at timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT product_attribute_values_pkey PRIMARY KEY (id)
);
CREATE TABLE public.product_category (
  id text NOT NULL,
  product_id text NOT NULL,
  category_id text NOT NULL,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT product_category_pkey PRIMARY KEY (id)
);
CREATE TABLE public.product_category_links (
  id text NOT NULL,
  product_id text NOT NULL,
  category_id text NOT NULL,
  valid_from timestamp without time zone,
  valid_to timestamp without time zone,
  position integer DEFAULT 0,
  is_featured boolean DEFAULT false,
  locale text DEFAULT 'en'::text,
  reason text,
  meta jsonb,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  deleted_at timestamp without time zone,
  CONSTRAINT product_category_links_pkey PRIMARY KEY (id)
);
CREATE TABLE public.product_images (
  id text NOT NULL DEFAULT (gen_random_uuid())::text,
  product_id text NOT NULL,
  url text NOT NULL,
  alt_text text,
  sort_order integer DEFAULT 0,
  is_primary boolean DEFAULT false,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT product_images_pkey PRIMARY KEY (id),
  CONSTRAINT product_images_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.product_recommendations (
  id text NOT NULL,
  product_id text NOT NULL,
  recommended_id text NOT NULL,
  score integer DEFAULT 0,
  position integer DEFAULT 0,
  locale text DEFAULT 'en'::text,
  meta jsonb,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT product_recommendations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.product_tag_links (
  id text NOT NULL,
  product_id text NOT NULL,
  tag_id text NOT NULL,
  valid_from timestamp without time zone,
  valid_to timestamp without time zone,
  position integer DEFAULT 0,
  is_featured boolean DEFAULT false,
  locale text DEFAULT 'en'::text,
  created_by text,
  reason text,
  meta jsonb,
  deleted_at timestamp without time zone,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT product_tag_links_pkey PRIMARY KEY (id)
);
CREATE TABLE public.product_variants (
  id text NOT NULL DEFAULT (gen_random_uuid())::text,
  product_id text NOT NULL,
  sku text UNIQUE,
  size text,
  color text,
  material text,
  price numeric,
  stock integer DEFAULT 0,
  image text,
  is_active boolean DEFAULT true,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT product_variants_pkey PRIMARY KEY (id),
  CONSTRAINT product_variants_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.product_views (
  id text NOT NULL,
  product_id text NOT NULL,
  user_id text,
  session_id text,
  viewed_at timestamp without time zone NOT NULL DEFAULT now(),
  CONSTRAINT product_views_pkey PRIMARY KEY (id),
  CONSTRAINT product_views_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.products (
  id text NOT NULL DEFAULT (gen_random_uuid())::text,
  name text NOT NULL,
  slug text NOT NULL UNIQUE,
  description text,
  price numeric NOT NULL,
  compare_at_price numeric,
  cost_price numeric,
  category_id text,
  brand text,
  sku text UNIQUE,
  barcode text,
  stock integer DEFAULT 0,
  low_stock_threshold integer DEFAULT 5,
  weight numeric,
  dimensions jsonb,
  images ARRAY,
  video_url text,
  tags ARRAY,
  is_active boolean DEFAULT true,
  is_featured boolean DEFAULT false,
  is_new boolean DEFAULT false,
  is_sale boolean DEFAULT false,
  meta_title text,
  meta_description text,
  meta_keywords text,
  rating_avg numeric DEFAULT 0,
  rating_count integer DEFAULT 0,
  view_count integer DEFAULT 0,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  in_stock boolean DEFAULT true,
  featured boolean DEFAULT false,
  short_description text,
  deleted_at timestamp with time zone,
  seo_title text,
  seo_desc text,
  CONSTRAINT products_pkey PRIMARY KEY (id),
  CONSTRAINT products_category_id_fkey FOREIGN KEY (category_id) REFERENCES public.categories(id)
);
CREATE TABLE public.promotions (
  id text NOT NULL,
  name text NOT NULL,
  description text,
  type text NOT NULL,
  value integer NOT NULL,
  position integer DEFAULT 0,
  is_featured boolean DEFAULT false,
  locale text DEFAULT 'en'::text,
  meta jsonb,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT promotions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.reviews (
  id text NOT NULL,
  user_id text NOT NULL,
  product_id text NOT NULL,
  rating integer NOT NULL,
  comment text,
  position integer DEFAULT 0,
  locale text DEFAULT 'en'::text,
  meta jsonb,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT reviews_pkey PRIMARY KEY (id)
);
CREATE TABLE public.sessions (
  id text NOT NULL,
  session_token text NOT NULL UNIQUE,
  user_id text NOT NULL,
  expires timestamp without time zone NOT NULL,
  CONSTRAINT sessions_pkey PRIMARY KEY (id),
  CONSTRAINT sessions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.settings (
  id integer NOT NULL DEFAULT nextval('settings_id_seq'::regclass),
  key text NOT NULL UNIQUE,
  value text NOT NULL,
  description text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT settings_pkey PRIMARY KEY (id)
);
CREATE TABLE public.site_config (
  id text NOT NULL,
  key text NOT NULL UNIQUE,
  value jsonb,
  description text,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT site_config_pkey PRIMARY KEY (id)
);
CREATE TABLE public.site_content (
  id text NOT NULL,
  type text NOT NULL,
  title text NOT NULL,
  slug text UNIQUE,
  content text,
  image_url text,
  published boolean DEFAULT false,
  sort_order integer DEFAULT 0,
  meta_description text,
  created_by text,
  created_at timestamp without time zone NOT NULL DEFAULT now(),
  updated_at timestamp without time zone NOT NULL DEFAULT now(),
  CONSTRAINT site_content_pkey PRIMARY KEY (id),
  CONSTRAINT site_content_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.users(id)
);
CREATE TABLE public.support_chat_messages (
  id text NOT NULL,
  session_id text NOT NULL,
  user_id text,
  message text NOT NULL,
  image_url text,
  sender text NOT NULL CHECK (sender = ANY (ARRAY['user'::text, 'admin'::text, 'ai'::text])),
  is_read boolean DEFAULT false,
  created_at timestamp without time zone NOT NULL DEFAULT now(),
  CONSTRAINT support_chat_messages_pkey PRIMARY KEY (id),
  CONSTRAINT support_chat_messages_session_id_fkey FOREIGN KEY (session_id) REFERENCES public.support_chat_sessions(session_id),
  CONSTRAINT support_chat_messages_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.support_chat_sessions (
  id text NOT NULL,
  session_id text NOT NULL UNIQUE,
  user_id text,
  user_email text,
  user_name text,
  status text NOT NULL DEFAULT 'active'::text CHECK (status = ANY (ARRAY['active'::text, 'resolved'::text, 'archived'::text])),
  assigned_to text,
  rating integer CHECK (rating >= 1 AND rating <= 5),
  feedback text,
  message_count integer DEFAULT 0,
  first_message text,
  last_message_at timestamp without time zone,
  resolved_at timestamp without time zone,
  resolved_by text,
  notes text,
  taken_over_by text,
  taken_over_at timestamp without time zone,
  ai_disabled boolean DEFAULT false,
  operator_rating integer CHECK (operator_rating >= 1 AND operator_rating <= 10),
  operator_rated_at timestamp without time zone,
  operator_rated_by text,
  created_at timestamp without time zone NOT NULL DEFAULT now(),
  updated_at timestamp without time zone NOT NULL DEFAULT now(),
  CONSTRAINT support_chat_sessions_pkey PRIMARY KEY (id),
  CONSTRAINT support_chat_sessions_assigned_to_fkey FOREIGN KEY (assigned_to) REFERENCES public.users(id),
  CONSTRAINT support_chat_sessions_operator_rated_by_fkey FOREIGN KEY (operator_rated_by) REFERENCES public.users(id),
  CONSTRAINT support_chat_sessions_resolved_by_fkey FOREIGN KEY (resolved_by) REFERENCES public.users(id),
  CONSTRAINT support_chat_sessions_taken_over_by_fkey FOREIGN KEY (taken_over_by) REFERENCES public.users(id),
  CONSTRAINT support_chat_sessions_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.tags (
  id text NOT NULL,
  name text NOT NULL,
  slug text NOT NULL UNIQUE,
  position integer DEFAULT 0,
  is_featured boolean DEFAULT false,
  locale text DEFAULT 'en'::text,
  meta jsonb,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT tags_pkey PRIMARY KEY (id)
);
CREATE TABLE public.user_product_interactions (
  id text NOT NULL,
  user_id text NOT NULL,
  product_id text NOT NULL,
  interaction_type text NOT NULL,
  position integer DEFAULT 0,
  locale text DEFAULT 'en'::text,
  meta jsonb,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT user_product_interactions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.user_profiles (
  id text NOT NULL,
  user_id text NOT NULL UNIQUE,
  first_name text,
  last_name text,
  phone text,
  address text,
  avatar text,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT user_profiles_pkey PRIMARY KEY (id)
);
CREATE TABLE public.user_wishlist_items (
  id text NOT NULL DEFAULT (gen_random_uuid())::text,
  user_id text NOT NULL,
  product_id text NOT NULL,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT user_wishlist_items_pkey PRIMARY KEY (id),
  CONSTRAINT user_wishlist_items_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(id)
);
CREATE TABLE public.users (
  id text NOT NULL,
  name text,
  email text NOT NULL UNIQUE,
  password text,
  email_verified timestamp without time zone,
  image text,
  role text DEFAULT 'user'::text,
  two_factor_secret text,
  last_edited_at timestamp without time zone,
  affinity_scores jsonb,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  status text NOT NULL DEFAULT 'active'::text,
  CONSTRAINT users_pkey PRIMARY KEY (id)
);
CREATE TABLE public.verification_tokens (
  identifier text NOT NULL,
  token text NOT NULL UNIQUE,
  expires timestamp without time zone NOT NULL
);
CREATE TABLE public.wishlists (
  id text NOT NULL,
  user_id text NOT NULL,
  name text DEFAULT 'Default'::text,
  position integer DEFAULT 0,
  is_featured boolean DEFAULT false,
  locale text DEFAULT 'en'::text,
  meta jsonb,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT wishlists_pkey PRIMARY KEY (id)
);